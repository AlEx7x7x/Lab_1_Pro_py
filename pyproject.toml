[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "online-store"
version = "0.1.0"
description = "Online store console application - Laboratory work No. 1, Variant 3"
requires-python = ">=3.11"

[project.scripts]
online-store = "online_store.main:main"
